package com.example;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class UploadServiceUT {
    @Test
    @DisplayName("Upload a file then read")
    void uploadAndRead(){
        // create signed post upload
        // check for function calls in the upload repository and object storage repository
        // notify the server about the upload end
        // check for calls in the upload repository
    }
}
