# Separating the upload transformer

Present the upload transforming tool as a proof-of-concept learning project. Otherwise, using it instead of Cloudinary
is not justifiable (?)

Maybe moving it to a separate git project and git repository will be necessary to make this intention clear. It would
also make the swapping easier.

Build a short migration plan for swapping to Cloudinary.